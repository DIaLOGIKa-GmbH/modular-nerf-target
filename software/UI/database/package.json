{
  "name": "scores",
  "version": "1.0.0",
  "description": "Contains the code the nerf-target score database",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3007",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
